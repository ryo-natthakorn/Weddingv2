Full-size originals of the pre-wedding photos.

Kept in the repo so the couple's unmodified files travel with the project,
but NOT shipped: the import glob in GallerySection.tsx is non-recursive, so
this subfolder never enters the bundle.

The optimised versions that guests actually load are one level up.
